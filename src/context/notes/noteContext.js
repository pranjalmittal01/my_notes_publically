// const { createContext } = require("react"); It is import in the format of CJS (Common JavaScript)
import { createContext } from "react";  // it is import in the format of ES Module (MJS)

const noteContext = createContext();

export default noteContext;